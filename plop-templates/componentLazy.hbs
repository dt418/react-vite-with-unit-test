import { lazy, Suspense } from 'react';

import { {{pascalCase componentName}}Props } from './{{pascalCase componentName}}';

const Lazy{{pascalCase componentName}} = lazy(() => import('./{{pascalCase componentName}}'));

const {{pascalCase componentName}} = (props: {{pascalCase componentName}}Props) => (
  <Suspense fallback={null}>
    <Lazy{{pascalCase componentName}} {...props} />
  </Suspense>
);

export default {{pascalCase componentName}};
